export const url = "https://norma.nomoreparties.space/api";
export const registerEndpoint = `${url}/auth/register`;
export const loginEndpoint = `${url}/auth/login`;
export const logoutEndpoint = `${url}/auth/logout`;
export const updateTokenEndpoint = `${url}/auth/token`;
export const userDataEndpoint = `${url}/auth/user`;

export const emailRegex = /[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,4}$/;